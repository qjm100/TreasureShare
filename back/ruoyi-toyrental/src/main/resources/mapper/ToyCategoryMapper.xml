<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.toy.mapper.ToyCategoryMapper">

    <resultMap type="ToyCategory" id="ToyCategoryResult">
        <id     property="id"         column="id"          />
        <result property="parentId"   column="parent_id"   />
        <result property="name"       column="name"        />
        <result property="sort"       column="sort"        />
        <result property="status"     column="status"      />
        <result property="createBy"   column="create_by"   />
        <result property="createTime" column="create_time" />
        <result property="updateBy"   column="update_by"   />
        <result property="updateTime" column="update_time" />
        <result property="remark"     column="remark"      />
    </resultMap>

    <sql id="selectCategoryVo">
        select id, parent_id, name, sort, status, create_by, create_time, update_by, update_time, remark
        from toy_category
    </sql>

    <select id="selectCategoryList" parameterType="ToyCategory" resultMap="ToyCategoryResult">
        <include refid="selectCategoryVo"/>
        <where>
            <if test="name != null and name != ''">
                AND name like concat('%', #{name}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by parent_id, sort
    </select>

    <select id="selectCategoryById" parameterType="Long" resultMap="ToyCategoryResult">
        <include refid="selectCategoryVo"/>
        where id = #{id}
    </select>

    <select id="selectCategoryTree" resultMap="ToyCategoryResult">
        <include refid="selectCategoryVo"/>
        where status = '0'
        order by parent_id, sort
    </select>

    <insert id="insertCategory" parameterType="ToyCategory" useGeneratedKeys="true" keyProperty="id">
        insert into toy_category (
            parent_id, name, sort, status,
            create_by, create_time, update_by, update_time, remark
        ) values (
            #{parentId}, #{name}, #{sort}, #{status},
            #{createBy}, sysdate(), #{updateBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updateCategory" parameterType="ToyCategory">
        update toy_category
        <set>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where id = #{id}
    </update>

    <delete id="deleteCategoryById" parameterType="Long">
        delete from toy_category where id = #{id}
    </delete>

    <select id="selectChildCountById" parameterType="Long" resultType="Integer">
        select count(1) from toy_category where parent_id = #{id}
    </select>

</mapper>
